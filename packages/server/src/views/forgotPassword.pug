html  
  head
  body
    div
      p Dear #{userProfile.username},
      br
      p We have received a request to reset the password for your #{company && company.name || appClientName} account.
      p Please click on the following link to complete the process:
      p 
        a(href=`${clientUrl}/#/resetpassword/${resetRequest.token}`) Click here for reset password
      br
      strong Please note the verification link will only be valid until #{moment(resetRequest.expiredAt).format('MMMM Do YYYY, h:mm a')}.
      br
      p If you did not request this password reset, please contact the #{company && company.name || appClientName} Help Team.
      br
      p
        strong Best Regards,
      span #{company && company.name || appClientName}
      br
      br
      br
    if(company && company.logo)
      img(style={'height':'60px', 'display':'block'}, src=`${baseUrl}/uploads/${company.logo}`)